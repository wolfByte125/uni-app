// import { fetchLabels } from "@/api/src/labels";

export default async function page() {
  // const fetchedLabels = await fetchLabels();

  // console.log(fetchedLabels);

  return <div>Dashboard</div>;
}
